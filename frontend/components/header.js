var React = require('react');

var Header = React.createClass({
  render: function () {
    return (
      <header>
        <span>I am the header</span>
      </header>
    );
  }
});

module.exports = Header;
